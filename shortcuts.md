# Shortcuts

## Serve the app locally

```bash
npm run outreach:dev
```

Opens the outreach tracker at http://localhost:3333 (Discover UI: http://localhost:3333/#/discover).

To sync contacts into the app before serving:

```bash
npm run outreach:serve
```
